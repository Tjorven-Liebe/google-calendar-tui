name: Release

on:
  push:
    tags:
      - 'v*' # Trigger bei Tags wie v1.0.0, v0.1.1 usw.

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libssl-dev pkg-config

      - name: Build Release Binary
        run: cargo build --release --locked

      # ... (Schritte nach dem Build)

      - name: Publish to Crates.io
        run: cargo publish --allow-dirty
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_TOKEN }}

      - name: Package Binary
        run: |
          cd target/release
          # Nutze hier den exakten Namen deiner Binary aus der Cargo.toml
          tar -czvf ../../google-calendar-tui-linux-x86_64.tar.gz rust_tui_calendar 
          cd ../..

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: google-calendar-tui-linux-x86_64.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}